/*esta intrução de include insere o conteúdo do arquivo stdio.h no programa*/
#include <stdio.h> 
#include <stdlib.h>
/*Primeiro programa em C*/

int main(){ /*todo programa em C deve possuir essa função principal*/
	printf("Olá Mundo");
		return (0);	/*esse retorno indica que o programa está finalizando, e coloca-se ZERO por significar que não há erros*/
	
	/*delimita o fim do conjunto de instruções da função main */
	
	return 0;
}
